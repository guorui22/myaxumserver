[workspace]
resolver = "2" # resolver字段用于配置依赖项解析器的版本，这里使用2版本
members = [
    "libconfig",
    "libproto",
    "metaforge"
]

[profile.release]
lto = true
strip = true
opt-level = "z"
codegen-units = 1
